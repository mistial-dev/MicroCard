{
  "date": "2026-09-17",
  "rust_tests": 157,
  "desktop_dotnet_differential_cases": 216,
  "deterministic_preprocessing": "passed",
  "msbuild_incremental_and_pin_rejection": "passed",
  "dotnet_rust_package_equality": "passed",
  "independent_scp03_client": "passed",
  "first_load_byte_mutation_fault_injection": "passed",
  "malformed_input_smoke_cases": 10000,
  "libfuzzer_target_build": "passed",
  "clippy_warnings_as_errors": "passed",
  "full_checkpoint": "passed on 2026-09-16 after the consolidated CC310 AES-CMAC batch: 157 Rust tests, 216 direct CIL differential cases, analyzer and managed-library acceptance, signed dependencies, SCP03, serial framing, adversarial restart persistence, fuzz-target compilation, workspace Clippy and exact default board budgets; sustained fuzzing was not run",
  "board_release_build": "passed",
  "board_flash_text_bytes": 302540,
  "board_initialized_data_bytes": 0,
  "board_bss_bytes": 196636,
  "cil_verifier_allocation": "focused gate passed: 257 control-flow states and pending offsets are pre-reserved fallibly, retained state stacks reserve exact bounded cells, and one reusable 256-slot work stack replaces repeated block-stack clones; 21 MC04 verifier tests and focused core Clippy pass; consolidated checkpoint passed",
  "remaining_mc04_verifier_allocation": "focused gate passed: local call-graph visit/depth arrays and per-method stack-height/pending-offset arrays reserve their complete validated bounds fallibly before signed-image activation or recovery; recursive graph and CIL validation tests plus core Clippy pass",
  "fallible_management_buffers": "focused gate passed: fixed GlobalPlatform discovery responses, complete TLV appends, registry templates and signed dependency-binding tables reserve bounded capacity before mutation; six focused discovery, registry, dependency and atomic quota tests plus core Clippy pass",
  "fallible_command_crypto_buffers": "focused gate passed: AID formatting, short-APDU encoding, inventory records and portable AES-CBC/CCM allocating wrappers reserve exact bounds before mutation; SCP03 KDF framing uses a fixed stack prefix plus borrowed CMAC parts; seven focused command, management and crypto checks plus core Clippy pass",
  "allocation_aware_transport_responses": "focused gate passed: domain-policy wire records, fixed status responses and INITIALIZE UPDATE output reserve complete bounds before mutation; the infallible exchange boundary returns an empty response if a two-byte failure status cannot be allocated; three focused transport, policy and SCP03 checks plus core Clippy pass",
  "fallible_portable_cbc": "focused gate passed: the allocating padded CBC API uses checked size arithmetic and one fallibly reserved zeroizing owner, SSD incarnation responses use exact fallible copying, and four focused CBC, failure-clearing, known-answer and SSD-creation checks plus core Clippy pass",
  "hardware_execution": "passed: initial DK smoke and post-reboot signer continuity; current firmware was not flashed",
  "dk_latency": null,
  "production_ready": false,
  "persistent_keystore": "passed: independent HMAC/CMAC, CBC/CCM, sharing, reboot, revocation rollback",
  "binary_uart_scp03": "passed",
  "serial_adapter_pty": "passed",
  "first_flash_artifacts": "preparation gate passed; final revision and hashes recorded in ignored artifacts/first-flash/manifest.json",
  "mp03_signed_dependencies": "passed: canonical ranges, provider policy, consumer pins, load order and unload guard",
  "mc04_structure": "passed: generated schema is current, zero-copy Rust structural parser and independent inspector fixture",
  "mc04_emission": "passed: deterministic 2510-byte Counter.mca after dead metadata removal, private-name stripping, token remapping, Rust/Python validation, 3072-byte ceiling",
  "mc04_signatures": "passed: allocation-free bounded ECMA signature validation, canonical compressed integers, profile types and remapped TypeDefOrRef targets",
  "mc04_opcodes": "passed: generated 114-opcode profile, host allowlist, borrowed Rust validation and independent inspection of boundaries, operands, compact tokens, switch quotas and branch targets",
  "kdf108_host": "passed: SCP03 SP800-108 AES-CMAC fixed vector, opaque SSD key handle, APDU boundary, valid MC04 provider/consumer, distinct Ed25519 signatures and exact ISD signer pin",
  "kdf108_device": "passed in portable runtime: signed ISD provider activation, verified SSD link and cross-assembly execution; physical DK scenario remains open",
    "roslyn_analyzers": "focused gate passed: Rider 2026.1 discovery plus enabled-by-default MCA0001 through MCA0025 authoring diagnostics; the complete negative-build corpus covers language, shape, call, dependency, lifecycle, transaction, allocation, structural quota, persistent declaration and persistent use-site failures; analyzer-disabled preprocessing and device/runtime enforcement remain authoritative",
  "auto_property_field_diagnostic": "focused gate passed: Rider and command-line builds report MCA0003 when an auto-property would synthesize storage outside the instance-Int32 field profile; analyzer-disabled byte-array compilation succeeds, preprocessing independently rejects the hidden field without output, and the static case plus ordinary corpus remain cleanly covered",
  "static_auto_property_diagnostic": "focused gate passed: Rider and command-line builds report MCA0003 when a static auto-property synthesizes forbidden backing storage; analyzer-disabled compilation succeeds, preprocessing independently rejects the static field flags without output, and the ordinary analyzer corpus rebuilds with zero warnings",
  "event_declaration_diagnostic": "focused gate passed: Rider and command-line builds report MCA0003 for event declarations; analyzer-disabled compilation succeeds as ordinary C#, preprocessing independently rejects the synthesized non-Int32 backing field without output, and the ordinary analyzer corpus rebuilds with zero warnings",
  "runtime_type_inspection_diagnostic": "focused gate passed: Rider and command-line builds report MCA0001 for typeof; analyzer-disabled compilation succeeds as ordinary C#, preprocessing independently rejects the InlineTok operand without output, and the ordinary analyzer corpus rebuilds with zero warnings",
  "method_local_limit": "passed: MCA0010 source feedback, analyzer-bypass preprocessing rejection and on-device MC04 verification at 64 locals",
  "method_parameter_limit": "focused gate passed: MCA0017, analyzer-disabled preprocessing and the on-device MC04 signature verifier independently enforce the 32-parameter maximum; exact-boundary fixtures pass and 33 parameters fail without output",
  "assembly_dependency_limit": "focused gate passed: MCA0018 and analyzer-disabled preprocessing enforce the signed 16-dependency maximum before output; the exact boundary passes, 17 fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "assembly_entry_point_limit": "focused gate passed: MCA0019 and analyzer-disabled preprocessing enforce the signed four-entry maximum before output; the exact boundary passes, five fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "mc04_method_row_limit": "focused gate passed: MCA0020 and analyzer-disabled preprocessing enforce the 256-MethodDef maximum before output; the exact boundary passes, 257 fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "mc04_type_row_limit": "focused gate passed: MCA0021 and analyzer-disabled preprocessing enforce the 253-TypeDef maximum including the module row before output; the exact boundary passes, 254 fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "mc04_field_row_limit": "focused gate passed: MCA0022 and analyzer-disabled preprocessing enforce the 1022-FieldDef maximum before output while excluding embedded Int32 constants; the exact boundary passes, 1023 fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "mc04_attribute_row_limit": "focused gate passed: MCA0023 and analyzer-disabled preprocessing enforce the 256-CustomAttribute maximum for retained runtime annotations before output; the exact boundary passes, 257 fail, and a correctly signed over-limit device load leaves live and recovered unbound/bound state unchanged",
  "constant_array_limit": "passed: MCA0011 rejects constant supported-array allocations above 16384 bytes and accepts the exact boundary",
  "loop_allocation_feedback": "passed: MCA0012 rejects explicit arrays, array-targeted collection expressions, and object allocation inside loops before preprocessing",
  "switch_target_limit": "focused gate passed: MCA0015 rejects 257 non-default statement or expression switch targets, accepts 256, analyzer-disabled preprocessing rejects dense 257-target CIL, and the device verifier accepts 256 while rejecting 257",
  "native_scratch_limits": "passed: key-service byte arguments are limited to 1024 bytes each and 2048 bytes combined before crypto execution",
  "native_crypto_provider": "focused gate passed: every VM-exposed SHA-256, Ed25519, HMAC, CMAC, CBC, CCM and P-256 operation routes through Platform CryptoProvider; fixed results use caller-owned outputs and zeroizing bridge vectors; AES block encryption is in-place; scatter-input CMAC removes SCP03 concatenation buffers; SCP03 verification consumes the parsed command and preserves its unencrypted payload allocation; CBC/CCM use caller-owned output with failed-plaintext zeroization and provider-length validation; pointer-identity tests prove stored keys and managed messages reach the provider without copies and the one result allocation moves through the key service into the VM heap; consolidated checkpoint passed",
  "hal_crypto_conformance": "focused gate passed: allocation-free provider runner verifies FIPS 180-4 SHA-256, RFC 4231 HMAC-SHA-256, RFC 4493 one-piece and segmented AES-CMAC, NIST SP 800-38A AES-128 block/CBC, NIST CAVP AES-CCM, RFC 8032 Ed25519 positive/changed-message behavior, and P-256 public-key, RFC 6979 deterministic ECDSA and fixed ECDH answers; negative cases cover complete caller-output clearing for short CBC/CCM buffers, malformed CBC ciphertext, padding/tag rejection, malformed verification inputs, invalid P-256 private-scalar and peer-key errors through the deterministic simulator provider",
  "board_crypto_inventory": "passed: codebase graph confirms nRF52840 is the only physical board backend; the inventory maps every required primitive to the default provider and any opt-in CC310 path, with explicit reasons for retaining Rust Ed25519, portable KDF framing and journal-backed protected keys",
  "transient_object_limits": "passed: 16384 charged bytes and 256 objects with no failed-reservation accounting drift",
  "platform_independent_object_charge": "focused gate passed: sealed objects charge a fixed 16 bytes per field plus a 16-byte header on simulator and nRF52840 builds; quota behavior no longer depends on Rust target layout",
  "simulator_runtime_peaks": "passed: KeyOperations HMAC/CMAC/CBC/CCM and KeyReader with bounded bulk command buffering measured 256 instructions, 7 evaluation slots, 10 locals, 2 frames, 177 transient bytes, 7 objects and 124 native work units; CI ceilings 512/8/16/4/256/8/128",
  "transaction_preprocessing": "passed: unsafe constructor path rejected with MCA0008 and independently rejected when analyzers are disabled",
  "invocation_atomicity": "passed: managed/native failure, fuel exhaustion and every modeled invocation-commit flash mutation recover old or complete new state",
  "transaction_package_copies": "passed: immutable signed envelopes retain pointer identity across cloned candidate states; canonical Base64 persistence fits Counter and KeyOperations in one SSD under the unchanged journal ceiling",
  "mp03_signature_verification": "passed: fixed v3 context embedded in signed header, one contiguous borrowed Ed25519 message, complete byte coverage, MP02 refusal and no upgrade path",
  "active_package_quota_bytes": 24576,
  "individual_package_quota_bytes": 16384,
  "host_package_quota": "passed: .NET signer accepts output above 8192 bytes and rejects output above 16384 bytes without writing a file",
  "package_activation_copy": "passed: successful activation moves the staging allocation; failed verification or commit restores it for retry",
  "signed_upload_allocation": "focused gate passed: GlobalPlatform and proprietary staged uploads reserve exact bounded growth before mutation, checked offsets reject overflow, persisted Base64 package recovery reserves fallibly, quota rejection preserves staged content and allocation identity, and focused core tests plus Clippy pass",
  "assembly_name_interning": "focused gate passed: package, version, binding, call and installed-instance maps share one Rc<str> per active assembly across live state and transaction snapshots; recovery re-interns active decoded keys and instance values while preserving standalone version high-water names after unload; canonical durable JSON is unchanged",
  "embedded_identifier_grammar": "focused gate passed: one 1-to-64-character ASCII grammar is enforced by the Rider analyzer, analyzer-independent preprocessor, signer, domain management, recovery and signed device verification; escaped, path-like, whitespace, Unicode, leading-punctuation and oversized identities are rejected before output, activation or first-load key binding",
  "globalplatform_management_identity_reuse": "focused gate passed: staged loads retain a fixed RegistryAid instead of a domain String; final activation and instance installation resolve borrowed canonical domain and assembly identities; native lifecycle JSON borrows canonical unescaped names from the command buffer; SSD deletion removes by borrowed identity and aborts its staged load; instance and assembly delete commands serialize borrowed identities without pre-cloning; 11 focused GlobalPlatform tests and core Clippy pass; production/development board text fell another 424/416 bytes",
  "owned_package_image_copy": "focused gate passed: owned package verification retains one signed-envelope allocation and a validated image range; pointer containment, exact image identity, package tests and focused Clippy pass",
  "journal_serialization": "passed: structural equality avoids serializing current state and protected key bytes use constant-time comparison",
  "journal_bounded_reads": "focused gate passed: portable flash uses checked offset reads; recovery reads fixed metadata and exact ciphertext instead of cloning 64 KiB slots; mapped nRF52840 erased-state inspection allocates nothing; CCM authenticates and decrypts the exact ciphertext allocation in place, clears failed plaintext and removes the tag before state decoding; measured regression coverage fixes the largest read to the committed record length",
  "selected_identity_reuse": "focused gate passed: plain and SCP03-verified processing plus ISD transitions move the selected domain/incarnation/AID tuple instead of cloning its strings; stale or successful ISD transitions clear selection, dispatch and deselect failures restore it, and pointer identity survives every restoration path",
  "invocation_identity_reuse": "focused gate passed: execution-unit discovery borrows canonical domain and assembly names; nontransactional calls allocate no owner identity, transaction begin creates one tuple, and later commands preserve both string-buffer addresses while moving that tuple between pending states",
  "five_minute_fuzz_campaigns": "passed without crash: 51463622 non-sanitized executions across boundaries, validly signed packages, crypto arguments and stateful domains; AddressSanitizer remains unavailable",
  "stateful_crypto_fuzzing": "passed without crash: three SSD assemblies, eight instances, deterministic HMAC/CMAC/CBC/CCM/stale-handle/byte-record/KDF seeds, 2329 mutated sequences, 3910 edges and 19535 features under a 65536-byte journal",
  "stateful_dependency_fuzzing": "passed: ISD Kdf108 provider and differently signed SSD consumer, active-consumer unload guard, consumer revocation, provider package-version replacement, verified relink and reinstall",
  "linked_verifier_limits": "passed: 17 assemblies, 4352 methods and 2048 managed call edges are rejected before growth beyond their bounds",
  "resolved_link_bytes": "passed: digest-only provider identity produces a 59-byte provider binding; dependency-table indices reduce managed call targets to 61 bytes; bounded lookup borrows canonical domain and assembly keys",
  "mc04_call_graph": "passed: host recursion/depth feedback plus independent portable-runtime validation of local and linked depth, cycles and transaction effects",
  "assembly_terminology": "passed: AssemblyAttribute, entry_points, instances and Kdf108Consumer; forbidden legacy term absent from tracked paths and content",
  "iso7816_default_assembly": "focused gate passed: pure managed version 0.1.0, 44 desktop boundary assertions, MC04 device-format verification, no native capabilities, signed ISD provisioning, exact signer/version SSD dependency, text and binary SCP03 execution, wrong-signer rejection before binding, provider unload guard and reboot recovery",
  "encoding_default_assembly": "focused gate passed: pure managed DER version 0.1.0, 73 desktop canonicality/high-tag/nesting/ordering/boundary/overlap/failure assertions, 32-method MC04 schema-2 device verification, no native capabilities, 3605-byte assembly under a 4096-byte ceiling and 4039-byte representative package under a 5120-byte ceiling; signed ISD provisioning and differently signed exact-pinned SSD consumption exercise high-tag read/write, caller-scratch nested validation, SET OF order rejection, unload guard and reboot over text and binary SCP03",
  "cryptography_default_assembly": "focused gate passed: signed managed version 0.1.0 exposes SHA-256, strict Ed25519 verification, HMAC-SHA-256, AES-CMAC, AES-CBC, AES-CCM, caller-buffer random fill and opaque P-256 public-key/ECDSA/ECDH operations through the pinned framework ABI; the 1691-byte provider and differently signed SSD consumer pass exact-signer dependency linking, valid/invalid/malformed signature cases, RFC 6979, cross-implementation P-256 verification and ECDH, deterministic hash/MAC, CBC/CCM round trips, random-output and failure-clearing checks, unload guard and key continuity after reboot over text and binary SCP03",
  "security_default_assembly": "security checkpoint passed: the 680-byte managed version 0.1.0 facade resolves only exact framework ABI IDs 40 through 45; native tests cover salted incarnation-and-slot-bound PIN/PUK verifiers, constant-time comparison, retry blocking, unblock, PIN change, ownership, entropy failure, serialized persistence and invocation-scoped authorization; a differently signed 1384-byte SSD consumer passes wrong-signer rejection, durable PIN and PUK attempts followed by managed faults, retry exhaustion, unblock, PIN change, provider unload, cross-SSD denial and reboot over text and binary SCP03; every byte-level retry-floor journal interruption recovers the prior valid state or the complete consumed-attempt state",
  "credential_invocation_fixed_capacity": "focused gate passed: authorization and monotonic retry floors use fixed arrays sized to the eight-credential bound; duplicate authorization, revocation, minimum-floor merging, impossible ninth-slot rejection, durable failure commit and retry reduction pass exact tests without transient tree-node allocation; core and both board Clippy configurations, fuzz-target compilation and exact board budgets pass; production/development text is 333916/333948 bytes with unchanged BSS",
  "single_buffer_vm_invocation": "focused gate passed: one exactly reserved fallible VM-unit vector replaces separate assembly-view and VM-unit vectors, removing up to 7344 bytes of duplicate 32-bit heap at 17 units; the full 248-byte response plus status is reserved before execution; exact linked-unit, response-bound, bulk-I/O and runtime-budget tests, core and both board Clippy configurations, fuzz-target compilation and board budgets pass; production/development text is 333756/333788 bytes with unchanged BSS",
  "single_queue_dependency_discovery": "focused gate passed: the exactly reserved 17-unit table is also the dependency work queue, removing up to 884 bytes of 32-bit heap; canonical identity deduplication, conflicting digest rejection, capacity, borrowed-unit and linked-effect tests plus core Clippy and board budgets pass; production/development text is 333396/333420 bytes with unchanged BSS",
  "reused_signature_scratch": "focused gate passed: caller-owned verifier scratch and one VM scratch vector replace per-call method-parameter allocation, per-frame parameter/local-type vectors and the verifier's copied argument vector; parsers clear scratch before rejection and enforce 32-parameter/64-local bounds; assembly, VM, linked-call and managed-call tests, core and both board Clippy configurations, fuzz-target compilation and board budgets pass; production/development text is 332916/332948 bytes with unchanged BSS",
  "persistent_credential_container_spike": "rejected after measurement: an inline sorted eight-entry container preserved canonical map serialization, capacity rejection, recovery and signed Credential acceptance but raised development board text from 333948 to 341204 bytes and enlarged every domain snapshot; the experiment was reverted",
  "default_bundle_manifest": "focused gate passed: deterministic signed MDB1 ownership manifest pins one ISD incarnation and signer plus the exact identity, assembly version, package version, MC04 image digest and complete signed-package digest of each default assembly; package order is dependency-first from mscorlib; exact verification accepts shuffled inputs and rejects signature damage, omissions and mixed signers; runtime dependency bindings already reject provider removal and replacement while in use",
  "focused_acceptance_inputs": "passed: Core, ISO 7816, Encoding, Cryptography, Security, Kdf108, Credential and default-bundle scenarios regenerate their own current MC04 provider and consumer inputs before signing; independently invoked focused tests no longer depend on full-checkpoint artifacts",
  "credential_acceptance": "focused simulator gate passed: 1607-byte MC04 Credential assembly with two exact-version, package-version and ISD-signer dependencies in its signed manifest; signed loading, transactional public-data provisioning, opaque P-256 key continuity, externally verified challenge signatures, retry consumption/reset/exhaustion, PUK unblock, provider unload protection, reboot, deletion, old-incarnation replay refusal and clean recreation passed; physical DK interruption scenario remains open",
  "credential_profile_budgets": "focused deterministic gate passed: four active signed packages occupy 6723 bytes and provisioned canonical journal state is 13682 bytes; maximum Credential paths use 602 instructions, 13 evaluation slots, 5 active locals, 3 frames, 239 transient bytes, 5 objects and 714 native work units; generated aggregate ceilings cover the five-assembly default ISD bundle and four-assembly Credential deployment",
  "core_library": "focused gate passed: Rider-safe MicroCard.Core builds and emits device identity mscorlib; explicit AssemblyRef rewriting is unique and verified; host-only identity/dependency metadata is stripped; 42 desktop assertions and signed differently keyed SSD execution cover bounded offset operations, endian conversion, exact ISD signer/version binding, unload denial and reboot; 2161-byte MC04 and 2585-byte representative package remain within raised aggregate profile ceilings; consolidated checkpoint passed",
  "platform_assembly_identity": "focused gate passed: MC04 schema 2 retains AssemblyRef public-key/token, culture and hash fields; every corpus framework reference carries the reproducible caller-approved framework DLL SHA-256, System.Runtime carries its exact public-key token, and ordinary managed dependencies carry neutral identity extras; the CLI and correctly signed staged loads reject modified identities before SSD binding; schema 1 is rejected with no conversion path",
  "reference_equality_branches": "passed: portable runtime beq and bne.un accept verified managed references while ordered comparisons still reject references; signed EncodingConsumer exercises both distinct-buffer and same-buffer overlap paths through a cross-assembly call",
  "bulk_command_io": "focused gate passed: bytewise native IDs 0 and 1 removed; command length/copy and response write use IDs 11 through 13, validate complete ranges before work-budget or visible-state changes, charge by bytes moved, and pass Counter, Kdf108 and ISO 7816 over text and binary SCP03 transports; fuzz targets build without a sustained campaign",
  "apdu_data_borrowing": "focused gate passed: short APDU parsing borrows command data from the transport buffer; plaintext SCP03 MAC verification truncates the borrowed view without allocation and preserves pointer identity through authenticated dispatch; encrypted levels retain one provider output allocation",
  "decrypted_command_zeroization": "focused gate passed: owned secure-message plaintext is cleared when its command leaves scope; borrowed transport commands remain immutable; text and binary SCP03 acceptance plus host and nRF52840 Clippy pass",
  "portable_hal_conformance": "focused gate passed: deterministic simulator covers stable caller-buffer identity, reset reporting, seeded entropy and failure clearing, capability denial, monotonic clock and watchdog expiry, fragmented command receipt, exact response transmission, partial flash persistence across simulated power loss, zero-to-one programming rejection, and provider SHA-256; physical nRF52840 adapter remains open",
  "adversarial_signing_simulator": "passed over binary SCP03: invalid and missing signatures, signed-field tampering, public-key substitution, wrong context/domain/incarnation, missing dependency, invalid capability/version/image/canonical form, wrong bound key, rollback, changed same-version content and old-incarnation replay all preserve exact journal hashes and inventory immediately and after a fresh simulator process",
  "domain_inventory_client": "passed: authenticated parser accepts the ISD plus eight SSDs, preserves canonical order across restart, rejects overflow, and provides read-only empty-bound prefix filtering",
  "globalplatform_simulator": "focused gate passed: official stable GlobalPlatformPro v25.10.20 artifact is pinned by size, SHA-256, tag/source identity and reproducible-build manifest; it performs Card Data discovery, SCP03 level 13, registry listing, SSD creation, sequential C4-wrapped MP03 loading, secured standard installation, instance and assembly removal, and SSD deletion; authenticated durable-state counts confirm each transition",
  "ccid_codec": "focused gate passed: allocation-free one-slot short-APDU codec accepts power, status, transfer and abort messages, borrows APDU bodies, preserves response slot/sequence, rejects malformed or oversized input before dispatch, emits the fixed ATR profile, and compiles in the boundary fuzz target without a sustained campaign",
  "ccid_slot_state": "focused gate passed: eight CCID tests cover inactive transfer rejection, power-on ATR, one-command busy rejection, exact completion sequence, both abort-pair arrival orders, mismatched aborts and disconnect cleanup; focused microcard-core Clippy and the consolidated checkpoint pass; sustained fuzzing remains deferred",
  "ccid_usb_boundary": "focused gate passed: twelve tests cover the descriptor set, parameterized identifiers, bounded UTF-16 strings, every valid message length, packet termination, malformed reuse, borrowed responses, strict abort setup parsing, dispatch and completion failure atomicity, both abort orders and disconnect; focused microcard-core Clippy, boundary fuzz-target compilation and the consolidated checkpoint pass; sustained fuzzing remains deferred",
  "ccid_cancellation": "focused gate passed: USB is serviced before execution and every 64 managed instructions; one abort half keeps the command in flight and prevents redispatch after VM return, while a complete pair or USB loss returns a cancellation fault, rolls back candidate-state writes, preserves durable credential retry floors, tears down SCP03 and retains the abort response; twenty CCID tests, pre-execution and post-write cancellation, reopen persistence, GlobalPlatform loading, workspace compile, focused core/board Clippy and both nRF52840 CCID variants pass; physical timing remains open",
  "multi_command_transactions": "focused gate passed: signed compiled managed begin, commit and abort bind only through pinned native IDs 46 through 48; integer and byte records provide read-your-writes while durable state stays unchanged; commit survives reopen, and explicit abort, reboot, invalid sequencing, command-budget exhaustion, oversized input, selection, lifecycle use and irreversible output discard the candidate; Rider MCA0008 and the independent preprocessor reject explicit transaction paths to Hardware.Write, while a retained pre-check MC04 adversarial fixture proves authoritative runtime rejection; USB reset, CCID power cycle and USB power loss reset the endpoint; consolidated checkpoint passed and physical DK acceptance remains open",
  "native_crypto_known_answers": "focused gate passed: FIPS 180-4 SHA-256, RFC 4231 HMAC-SHA-256, all four RFC 4493 AES-CMAC cases including segmented input, NIST SP 800-38A AES-128 block/CBC first-block and NIST CAVP AES-CCM exact-profile vectors pass; two authorized independent Gp4Net vectors cover SCP03 session keys and card/host cryptograms; retained GlobalPlatformPro physical-card traces verify level-01 complete C-MAC chaining, level-03 counter-one decryption and level-11 response MAC; an independent Gp4Net level-13 vector verifies command encryption, full command chaining, plaintext recovery and response MAC; response-MAC provider failure zeroizes the session, and secret derivation/HMAC/CMAC/AES-block/ECDH scratch arrays zeroize after copying or error; physical level-13 GlobalPlatformPro capture, hardware-provider vectors and sustained fuzzing remain scheduled work",
  "scp03_level13_oracle": "focused gate passed: Gp4Net revision add447a082f3ac3d6cd0fc552052f3108912661c independently produced the fixed level-13 secured command, complete 16-byte command chain and secured response; Rust recovers the exact plaintext command, reproduces the exact response MAC, rejects a modified command MAC and tears down that session; 11 focused SCP03 tests and core Clippy pass",
  "nested_type_diagnostic": "focused gate passed: Rider analyzer reports MCA0003 for a nested sealed class, while analyzer-disabled preprocessing independently inspects source type ownership and rejects NestedClass metadata without producing or silently flattening an MC04 assembly",
  "declaration_shape_verification": "focused gate passed: Rider MCA0003 rejects custom inheritance, static or non-Int32 fields and constants, plus non-default method implementation flags; analyzer-disabled preprocessing rejects those cases plus source InterfaceImpl rows; the Rust MC04 verifier restricts bases to System.Object or no base and rejects forged interface-style or unsealed type flags, static or non-Int32 fields, virtual methods and native implementation flags before activation",
  "monotonic_journal_anchor": "focused gate passed: a backend-specific append-only sequence outside all MJ02 slots rejects older authenticated snapshots, erased slots and out-of-order generations; a journal one generation ahead recovers interrupted anchor advancement, capacity rejects before journal mutation, and post-commit anchor failure poisons the live journal until reopen; the nRF52840 programs one ordered 32-bit word per generation for 1024 commits in 4 KiB and rejects holes or malformed words; earlier counter formats have no conversion path",
  "fallible_managed_allocation": "focused gate passed: the evaluation stack and call-frame vectors reserve bounded capacity before managed execution, locals and transient objects reserve fallibly before mutation, and byte, Int32 and sealed-object backing buffers return Quota rather than invoking an infallible allocation path; seven interpreter tests, the representative runtime budget scenario, core and both board Clippy configurations, exact board budgets and the quick host gate pass",
  "fallible_native_buffers": "focused gate passed: opaque handles, hashes, MACs, CBC/CCM and P-256 results, SCP03 plaintext and responses, journal recovery, owned package envelopes and application blob copies reserve exact lengths fallibly; abandoned plaintext and replaced or deleted blob allocations are zeroized; 42 focused tests, core and both board Clippy configurations, exact board budgets and the consolidated checkpoint pass",
  "nrf52840_crypto_source_pin": "focused gate passed: the Nordic Oberon PSA Crypto ncs-v3.4.0 annotated tag, commit, Git tree, upstream version, two license files and twelve TF-PSA/CC3XX integration files match the checked-in lock; nrfxlib tag, commit, tree, selected archives, headers and licenses match their lock; sdk-nrf v3.4.0 tag, commit, tree, root license and two compiled P-256 driver sources match their separate lock",
  "nrf52840_cc310_platform_spike": "focused gate passed: opt-in documented SHA-256 initializes CC310 without RNG, uses fixed 16-byte aligned DMA scratch and clears caller output on every error; entropy uses exact-output failure clearing; AES-CMAC, HMAC-SHA-256, AES-128 block, CBC and CCM operations compile the pinned TF-PSA wrapper and P-256 also compiles two exact sdk-nrf CC3XX driver sources; build pins verify the 35-file MAC, 36-file cipher, 37-file AEAD and 48/49/50-file P-256 closures; all operations borrow inputs, publish only exact successful results, wipe complete caller output on failure and link no exact C allocator symbols; boot includes RFC 4493, RFC 4231, empty HMAC, FIPS 197, SP 800-38A, CAVP CCM, exact RFC 6979 public-key/signature verification, changed-message rejection and fixed ECDH answers; P-256 adds 17344 flash and 1200 aggregate RAM bytes, with 88 to 152 bridge-owned stack bytes before unmeasured vendor callees; the combined provider image uses 335836 text and 197840 aggregate RAM bytes under its 375000/200000 experimental ceilings; all features remain disabled pending physical and forced-failure evidence",
  "domain_application_zeroization": "focused gate passed: dropping a transaction snapshot or domain zeroizes every application Int32 value and zeroizes and clears every byte-record allocation; direct wipe and durable GlobalPlatform deletion tests pass, while native keys and credential verifiers retain independent drop zeroization",
  "fallible_dependency_linking": "focused gate passed: MemberRef uses are canonicalized in one pre-sized vector, dependency queues reserve the 17-unit limit once, resolved calls reserve the exact use count, and linked method-state, offset and edge buffers allocate fallibly within their verified bounds; import, borrowed-unit, distinct-signer dependency, linked-limit, core and board Clippy, exact board budget and quick host gates pass",
  "init_accessor_diagnostic": "focused gate passed: Rider and command-line analysis report MCA0001 for C# init accessors; analyzer-disabled compilation remains valid C#, while preprocessing rejects the required custom modifier with a stable diagnostic and produces no assembly, manifest or debug mapping; the ordinary analyzer corpus rebuilds with zero warnings; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "by_reference_return_diagnostic": "focused gate passed: Rider and command-line analysis report MCA0003 for by-reference method returns; analyzer-disabled compilation remains valid C#, while preprocessing rejects the by-reference signature with a stable diagnostic and produces no assembly, manifest or debug mapping; the ordinary analyzer corpus rebuilds with zero warnings; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "simulator_flash_scratch": "focused gate passed: file-backed simulator initialization, erase and erased-state scans use one fixed 4 KiB scratch buffer; programming validates borrowed input through a complete chunked read pass before mutation; two focused tests prove cross-chunk rejection atomicity and simulator Clippy passes with warnings denied; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "internal_management_tuple": "focused gate passed: GlobalPlatform instance and assembly deletion dispatch validate embedded identifiers and write their canonical tuple into one exactly requested fallible buffer instead of invoking the JSON serializer; the encoder round-trip, complete registry/deletion scenario and core Clippy pass; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "activation_staging_restore": "focused gate passed: failed activation drops the candidate state and reclaims the uniquely owned signed-package vector without a full-package copy; an impossible remaining owner fails closed as Storage; focused journal-failure coverage proves byte and pointer identity and core Clippy passes; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "journal_record_allocation": "focused gate passed: MJ02 commit checks plaintext, tag, record and durable u32 lengths, then reserves the complete record fallibly before writing its header, invoking AES-CCM or mutating flash; encrypted round-trip, wrong-key rejection and core Clippy pass; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "persistent_identity_allocation": "focused gate passed: required persistent domain and AID strings reserve exactly and fallibly; pending transactions and credential retry-floor commits use fixed registry AID plus incarnation instead of copied domain names; domain-policy parsing reserves its exact identifier and capability count; focused policy, selection, transaction, instance and credential tests plus core Clippy pass; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "explicit_durable_policy": "focused gate passed: every MJ02 domain carries an explicit policy; standard construction reserves all 44 capabilities fallibly, and omitted-policy snapshots are rejected without migration; fresh-domain reboot and core Clippy pass; the current Credential journal baseline is recorded separately; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "bounded_recovery_identities": "focused gate passed: authenticated recovery reserves one 97-entry registry-AID vector and one 72-entry linked-root vector, uses linear duplicate checks, and rejects the seventeenth instance before insertion; inconsistent snapshots, domain reboot, maximum instance enforcement and core Clippy pass; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "fixed_key_nonce_validation": "focused gate passed: protected-key recovery validates its maximum eight nonce identities in one stack array and preserves algorithm-specific key checks; duplicate nonces fail closed, a distinct replacement validates and core Clippy passes; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "text_simulator_apdu_boundary": "focused gate passed: text input is bounded to the 261-byte short-APDU ceiling before exact fallible allocation, output uses exact fallible allocation and canonical uppercase encoding without per-byte formatting allocations, malformed and over-boundary input is rejected, and simulator Clippy passes; consolidated checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "compact_ssd_registry": "focused gate passed: the persistent SSD registry uses one sorted vector with fallible growth, recovery reserves its complete eight-entry capacity and rejects duplicate or ninth keys before domain-value decoding, canonical JSON and lexical inventory order are preserved, all eight domains survive reboot, and focused snapshot, inconsistent-recovery and core Clippy checks pass; candidate-state allocator injection, consolidated checkpoint and sustained fuzzing remain deferred",
  "compact_integer_store": "focused gate passed: persistent integer records use numerically sorted vectors with fallible sixteen-entry growth, mutation and recovery enforce 512 records, duplicate or 513th keys fail before value decoding, replacement and partial recovery drop zeroize old values, canonical JSON remains stable, and focused quota, rollback, multi-command transaction and core Clippy checks pass; candidate-state allocator injection, consolidated checkpoint and sustained fuzzing remain deferred",
  "compact_byte_store": "focused gate passed: persistent byte records use numerically sorted vectors with fallible eight-entry growth, mutation and recovery enforce 64 records, duplicate or 65th keys fail before value decoding, rejected, replacement and partially recovered buffers are wiped, accepted buffers move without copying, canonical JSON remains stable, and focused quota, native API, transaction, zeroization and core Clippy checks pass; candidate-state allocator injection, consolidated checkpoint and sustained fuzzing remain deferred",
  "compact_protected_key_store": "focused gate passed: protected keys use one numerically sorted vector with exact next-entry reservation before entropy generation or recovery value decoding, duplicate and out-of-range recovery slots fail before secret decoding, canonical JSON and zeroization remain, and focused persistence, revocation, nonce, P-256, provider-boundary, structural-equality and core Clippy checks pass; candidate-state allocator injection, consolidated checkpoint and sustained fuzzing remain deferred",
  "compact_credential_store": "focused gate passed: credential verifiers use one numerically sorted vector with exact next-entry reservation before entropy or recovery value decoding, duplicate, negative and ninth keys fail before verifier decoding, transparent canonical JSON and retry behavior remain, candidate and replacement digest/salt scratch is zeroized, old verifier fields are wiped before replacement, and focused ordering, quota, serialization, retry, entropy-failure, retry-floor reopen and core Clippy checks pass; byte-level power-loss, consolidated checkpoint and sustained fuzzing remain deferred",
  "compact_persistent_map_batch": "quick host gate passed in 14.2 seconds after SSD, integer, byte, protected-key and credential store conversions: Rust workspace check, clean managed builds, 44 ISO 7816 assertions, 73 DER assertions and 42 bounded core-library assertions; focused core Clippy and store-specific tests pass; exact board linking, full checkpoint and sustained fuzzing remain deferred",
  "compact_instance_registry": "focused gate passed: installed instances use one lexically sorted eight-entry vector per domain, mutation reserves the exact next entry and owned AID before managed installation, duplicate and ninth recovery keys fail before assembly-name decoding, accepted entries share canonical assembly-name pointers, and focused ordering, capacity, selection, lifecycle failure, reboot, registry/deletion, snapshot-sharing and core Clippy checks pass; board linking later passed, while full checkpoint and sustained fuzzing remain deferred",
  "compact_assembly_maps": "focused gate passed: package, version, dependency-binding and resolved-call maps use generic sorted vectors with exact next-entry preflight before activation; duplicate and ninth recovery keys fail before link or Base64 value decoding; canonical JSON, version history, in-place Rc name interning, staged-allocation rollback, dependencies, signed malformed-image rejection, execution borrowing and snapshot sharing pass; no production BTreeMap remains in microcard-core; production and development-debug nRF52840 links pass at 290612 and 290628 text bytes, zero data and 196636 BSS, reducing development text by 42288 bytes from the prior 332916 baseline; the 16.2-second quick host gate passes with 44 ISO 7816, 73 DER and 42 bounded core-library assertions; full checkpoint and sustained fuzzing remain deferred",
  "fallible_candidate_state_clone": "focused gate passed: every production transactional state clone now shares immutable signed packages and reserves each owned map, record vector, nested byte buffer, domain identity, instance identity, dependency table, native-key store, credential store and policy buffer through a fallible clone context; deterministic failure injection rejects every reservation in a representative populated state with Quota and leaves the live serialized state unchanged; transaction, activation rollback, invocation rollback, selection and core Clippy checks pass; exact nRF52840 links pass with 292972 production and 293020 development text bytes, zero data and 196636 BSS; the full checkpoint and sustained fuzzing remain deferred",
  "copy_audit_and_streamed_mc04": "focused gate passed: the managed MC04 writer exposes Tables, Strings, Blob and Code as ReadOnlyMemory and streams them into a same-directory temporary file before atomic replacement, eliminating four section copies and a second complete assembly buffer; two emissions are byte-identical to each other and to the prior canonical Counter output; forced publication failure leaves the destination untouched and removes the temporary assembly; the fast copy audit fixes 20 reviewed bounded Rust copy sites across package verification, MC04 verification, linking and execution and rejects reintroduced whole-section writer copies; the 13.5-second quick host gate passes with 44 ISO 7816, 73 DER and 42 bounded core-library assertions; the full checkpoint and sustained fuzzing remain deferred",
  "managed_profile_enforcement_matrix": "audit gate passed: MCA0001 through MCA0025 are present in the enabled analyzer and checkpoint negative-build acceptance, every diagnostic has one matrix row mapping host feedback to authoritative signed-image or runtime enforcement, and checked host/device anchors now include persistent schema merging and per-call authorization; the full checkpoint and sustained fuzzing remain deferred",
  "signed_persistent_storage_schema": "focused gate passed: C# assembly attributes declare bounded Int32 and byte storage; MCA0024 rejects duplicate and invalid declarations; MCA0025 rejects dynamic, undeclared and wrong-kind storage calls; analyzer-disabled preprocessing, packager and Rust verifier independently validate the signed schema; domain activation pins the merged contract through unload and reboot, rejects conflicting redeclarations atomically, and every native call checks the executing assembly, domain incarnation, kind and byte bound; ISD dependency code cannot access caller SSD storage; immutable schema allocations are shared across candidate states and compact journal tuples fail closed on older object-form state; focused runtime, recovery, snapshot and budget tests, core Clippy, fuzz-target compilation, both nRF52840 release links and the 13.8-second quick gate pass; production/development text is 300764/300788 bytes with zero data and 196636 BSS; the full checkpoint and sustained fuzzing remain deferred",
  "maximum_capacity_candidate_clone": "focused gate passed: one domain fills all eight assembly, binding, call, version and instance slots, 512 integer records, 64 byte records totaling 8192 bytes, eight protected keys, eight credential verifiers and 512 pinned declarations; a successful clone shares immutable buffers, every owned reservation fails in turn with Quota, and the serialized live state remains byte-for-byte unchanged after every partial candidate is discarded; the focused test completes in 0.12 seconds and core Clippy passes; the full checkpoint and sustained fuzzing remain deferred",
  "nrf52840_flash_package_staging": "focused gate passed: the runtime accepts interchangeable RAM and flash upload backends; nRF52840 uploads program one of four 16 KiB staging banks directly, duplicate chunks compare through 64 bytes of stack scratch, successful activation moves the one exact materialized package allocation into persistent state, and validation or journal failure leaves the flash bytes retryable; the RAM backend retains pointer-identity activation; staging integration tests, core Clippy, the quick host gate, copy audit and both board links pass; production/development text is 301508/301532 bytes with zero data and 196636 BSS; the full checkpoint, physical flash interruption sweep and sustained fuzzing remain deferred",
  "three_slot_journal_wear": "focused gate passed: the portable MJ02 journal validates and retains a stable two-to-eight-slot backend count, rotates to the next slot and preserves reclaim markers; a three-slot host model reaches generations one, two and three without recycling, places generation four in slot zero, and recovers generation three or four at every modeled mutation during reuse; the nRF52840 uses three 64 KiB slots and a 4 KiB programmed-once word anchor with no conversion path; 19 focused journal tests, core Clippy and both board budget links pass; production/development text is 301532/301556 bytes with zero data and 196636 BSS; the full checkpoint, physical NVMC interruption sweep and sustained fuzzing remain deferred",
  "managed_sha256_offsets": "focused gate passed: standard-cased SHA256.HashData accepts source and destination arrays with offsets and source length and returns the fixed 32-byte written count; the trusted native ABI validates both ranges before provider dispatch, borrows the input, supports aliased managed arrays, copies only the fixed 32-byte stack result into the managed destination and allocates no VM result object; desktop .NET covers nine output, count, alias, boundary-preservation and rejection assertions, signed interpreted execution matches the known digest through distinct and aliased arrays and rejects both invalid ranges, the complete cryptography facade resolves capability 49, every retained MC04 fixture verifies against the new framework pin, the independent crypto and SCP03 scenarios pass, Credential budgets are 6794 active package bytes and 13827 journal bytes, core Clippy and the 13.1-second quick host gate pass, and production/development board links use 302028/302044 text bytes, zero data and 196636 BSS; the full checkpoint and sustained fuzzing remain deferred",
  "system_sha256_projection": "focused gate passed: Rider permits only System.Security.Cryptography.SHA256.HashData(byte[]); independent preprocessing matches the exact metadata signature, rewrites the BCL type and member to pinned MicroCard.Framework.Cryptography.Sha256, records capability 20 and omits the BCL cryptography AssemblyRef; the unsupported stream overload emits MCA0004 and is independently rejected with analyzers disabled; Rust inspection resolves the emitted call only as native 20, signed crypto acceptance matches an independent digest, all sixteen assembly/package budgets and the 13.9-second quick gate pass; the full checkpoint and sustained fuzzing remain deferred",
  "system_random_bytes_projection": "focused gate passed: Rider permits only System.Security.Cryptography.RandomNumberGenerator.GetBytes(int); independent preprocessing matches the exact metadata signature, rewrites the BCL type and member to pinned MicroCard.Framework.Cryptography.RandomBytes, records capability 50 and omits the BCL cryptography AssemblyRef; the runtime accepts zero through 1024 bytes, reserves one exact result vector, charges deterministic work before provider dispatch, returns zero-length requests without provider-dependent behavior, clears partial output on failure and moves successful ownership into the zeroizing VM heap; other RNG members emit MCA0004 and fail with analyzers disabled; desktop .NET passes fourteen cryptography assertions, signed execution returns 32 native bytes and rejects 1025, all retained images and budgets, independent crypto and SCP03 acceptance, core Clippy, both board links and the 15.0-second quick gate pass; production/development text is 302460/302476 bytes with zero data and 196636 BSS, Credential active packages/journal are 6858/13951 bytes; the full checkpoint and sustained fuzzing remain deferred",
  "native_fixed_time_equals": "focused gate passed: MicroCard.Cryptography.CryptographicOperations.FixedTimeEquals exposes whole-array and offset-length forms backed by exact framework capability 51; native execution validates and borrows both ranges without copying, bounds each to 1024 bytes, charges by the larger range and uses subtle constant-time equality for equal-length inputs; desktop .NET passes 21 cryptography assertions, signed simulator acceptance covers equal and unequal same-buffer ranges plus invalid-range session teardown and reboot, all 11 retained MC04 images verify under framework pin d4232124b7e7b13d23a6516b124e90561009ef90da5541893315b373442c265d, 16 assembly and package budgets pass, core Clippy is clean, the 14.8-second quick gate passes, and production/development board links use 303060/303084 text bytes with zero data and 196636 BSS; the full checkpoint and sustained fuzzing remain deferred",
  "provider_failure_output_contract": "focused gate passed: software CBC and CCM, simulator entropy, RuntimePlatform randomness and every opt-in CC310 fixed or variable output now clear the complete caller-owned buffer on setup, bounds, driver, authentication and padding failures; deterministic conformance covers short CBC/CCM outputs and malformed inputs, direct helper coverage checks success/error behavior, core and simulator Clippy pass, both fallback and complete CC310 board configurations pass Clippy, source pins and exact stack shapes pass, production/development board links remain within budget at 302284/302308 text bytes with zero data and 196636 BSS, and the 14.2-second quick host gate passes; physical forced-failure observation, the full checkpoint and sustained fuzzing remain deferred under the validation cadence",
  "nrf52840_ownership_marker": "focused gate passed: a one-way word at key-page offset 32 binds the nRF52840 management-key erase page to durable state before the first journal commit; fresh blank provisioning is accepted, established ownership requires nonblank durable state, markerless state has no migration path, and any partial marker program counts as ownership; all failure branches after key construction explicitly drop management keys, and the derived journal key moves through a non-copying zeroizing owner into the journal; four portable state-machine tests, nineteen journal tests and the key-derivation test pass; core and simulator plus fallback/complete-CC310 board Clippy pass; exact production/development links use 302540/302564 text bytes with zero data and 196636 BSS, and all CC310 link/stack evidence remains within ceilings; physical interruption and debug-lock acceptance remain open, while the quick host gate, full checkpoint and sustained fuzzing remain deferred under the validation cadence"
}
